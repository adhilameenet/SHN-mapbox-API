<section>
    <div class="container mt-2 pt-5">
        <div class="row">
            <div class="col-12 col-sm-8 col-md-6 m-auto">
                <h6 class="text-center">Click here to find your latitude and longitude coordinates</h6>
                <button class="w-100 btn btn-dark" onclick="getLocation()">Click</button>
                <div class="result mt-3">
                    <span id="demo1"></span><br>
                    <span id="demo2"></span> 
                </div>
                <div class="mt-3 w-100 alert alert-dark" role="alert">
                    <p class="alert-content">Just copy and past your latitude and longitude on corresponding input field</p>
                </div>
                <div class="card border-0">
                    <div class="card-body">
                        <form action="/" method="post" autocomplete="off">
                        <label for="demo1">Latitude*</label>
                            <input type="text" name="latitude" id="demo1" class="form-control py-2" required>
                                <label for="demo2">Longitude*</label>
                            <input type="text" name="longitude" id="demo2" class="form-control py-2" required>
                            <div class="text-center">
                                <input type="submit" class="w-100 mt-3 btn btn-dark" value="Add Coordinates">
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>


<script>

    var lat = document.getElementById("demo1");
    var lon = document.getElementById("demo2");

    function getLocation() {
        if (navigator.geolocation) {
            navigator.geolocation.getCurrentPosition(showPosition);
        }
        else {
            x.innerHTML = "Geolocation is not supported by this browser.";
        }
    }

    function showPosition(position) {
        lat.innerHTML = "Latitude : " + position.coords.latitude;
        lon.innerHTML = "Longitude :  " + position.coords.longitude;
    }



</script>